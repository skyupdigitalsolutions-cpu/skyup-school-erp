'use strict';
// Student/parent read-only events feed — no schema of its own, reads the
// existing staff `Event` model through a strict public-field projection.
module.exports = require('./routes');
