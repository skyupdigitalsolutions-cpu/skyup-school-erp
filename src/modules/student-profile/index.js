'use strict';
// Student-profile has no schema of its own — it operates on the existing
// Student model (registered by the principal module) via studentScope.js.
module.exports = require('./routes');
