'use strict';
// Student-timetable has no schema of its own — it's a read-only,
// student-scoped view over the existing TimetableEntry model (timetable module).
module.exports = require('./routes');
